requests>=2.31.0
# 以下为可选增强，按需安装：
# jieba>=0.42.1        # 中文分词（Archive.py 可选）
# scikit-learn>=1.3.0   # 语义匹配（意图识别可选）
# beautifulsoup4>=4.12.0 # HTML 解析（Nuwa.py 可选）
# pyyaml>=6.0           # YAML 配置（可选）
