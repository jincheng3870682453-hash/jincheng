# Tags: markdown, doc, format
# Desc: Markdown 文档格式规范

## 文档结构

1. 标题层级清晰（H1 一个，H2 分模块，H3 分细节）
2. 代码块必须标注语言
3. 表格对齐，不超过 5 列
4. 列表不超过 3 层嵌套

## 正确示例

```markdown
# 项目标题

## 安装

```bash
pip install -r requirements.txt
```

## 配置

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `timeout` | 30 | 超时秒数 |
| `retries` | 3 | 重试次数 |

## 注意事项

- 生产环境必须设置 `API_KEY`
- 日志级别建议设为 `INFO`
```

## 禁止模式

- [TODO]、[待补充]、[占位符] 等未填内容
- 超过 5 列的表格
- 没有语言标注的代码块
- 标题跳跃（H1 直接到 H3）
- 行长度超过 120 字符
